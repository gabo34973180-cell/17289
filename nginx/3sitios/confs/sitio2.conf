events {
    worker_connections 1024;
}

pid logs/sitio2.pid;

http {
    server {
        listen 82;
        server_name localhost;

        location / {
            root /mnt/c/Users/gabo3/desWeb/17289/nginx/3sitios/sitio2;
            index index.html;
        }
    }
}
